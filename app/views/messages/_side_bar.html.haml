.side-bar
  .side-header
    %h2.side-header__user-name
      = current_user.name
    %ul.side-header__lists
      %li.side-header__list
        = link_to do
          = icon('fa', 'edit', class: 'icon')
      %li.side-header__list
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog', class: 'icon')
  .groups
    .group
      .group__group-name
        members
        .group__latest-message
          まだメッセージがありません
    .group
      .group__group-name
        members
        .group__latest-message
          まだメッセージがありません